# Firewall_API
A server program I made in C for an assignment which implements an API to maintain a firewall configuration. The server maintains a collection of firewall rules, which specify what IP address-port pairs are allowed; a list of all requests submitted to it; and, for each rule stored, a list of IP address-port pairs that have been previously queried and found to match this rule.
